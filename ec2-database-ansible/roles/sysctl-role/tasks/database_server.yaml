- name: Create sysctl file
  ansible.builtin.copy:
    src: database-sysctl.conf
    dest: /etc/sysctl.d/database-sysctl.conf
    mode: '0644'
    owner: root
    group: root
  notify:
    - Reload sysctl
